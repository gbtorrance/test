package org.tdc.modelinst;

/**
 * Builder responsible for doing the complex work of constructing and assembling a {@link ModelInst} instance.
 */
public interface ModelInstBuilder {
	ModelInst build();
}
