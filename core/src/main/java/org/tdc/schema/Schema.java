package org.tdc.schema;

/**
 * Light-weight representation of a version of a schema on the filesystem.
 * 
 * Schemas are only represented structurally in memory with Models objects ({@link org.tdc.modeldef.ModelDef ModelDef} and {@link org.tdc.modelinst.ModelInst ModelInst}).
 * Schema objects are only really used at this point as a way to group and organize Models.  
 */
public interface Schema {

}
